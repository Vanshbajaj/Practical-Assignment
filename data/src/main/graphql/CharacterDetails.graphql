# CharacterDetails.graphql
query CharacterDetails($id: ID!) {
    character(id: $id) {
        name
        status
        species
        gender
        image
        origin {
            name
            dimension
        }
        episode {
            id
            name
        }

    }
}


