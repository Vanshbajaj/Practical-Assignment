# detekt.yml
detekt:
  config: ''

  build:
    maxIssues: 0
    failFast: false
    parallel: true
    profile: default

  formatting:
    active: true
    android: false

  rules:
    complexity:
      ComplexMethod:
        active: true
        threshold: 10
